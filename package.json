{
  "name": "resumap",
  "version": "1.0.0",
  "description": "ResuMap - AI-powered resume analysis platform",
  "private": true,
  "scripts": {
    "build": "cd hirelens-api && npm run build",
    "dev:api": "cd hirelens-api && npm run dev",
    "dev:mobile": "cd hirelens-mobile && npm run dev",
    "test": "cd hirelens-api && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
